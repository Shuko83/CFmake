# This is a basic Config file for find_package() generated by CSToolkit.
# 
# This file was generated by the use of LEGACY option with cstoolkit_fetch_artifactory.
# 
# If the package does not implement the standard expected by this file 
# you can write your own Find@fetch_package_name@.cmake file and add its folder to CMAKE_MODULE_PATH
#   list(APPEND CMAKE_MODULE_PATH "${CMAKE_SOURCE_DIR}/cmake")

set(@fetch_package_name@_ROOT_DIR "${CSTOOLKIT_EXTERNALS}/@fetch_package_name@")

file(GLOB_RECURSE _LIB_LIST "${@fetch_package_name@_ROOT_DIR}/*.lib")
list(FILTER _LIB_LIST INCLUDE REGEX "/[Rr]elease/")

file(GLOB_RECURSE _DLL_LIST "${@fetch_package_name@_ROOT_DIR}/*.dll")
list(FILTER _DLL_LIST INCLUDE REGEX "/[Rr]elease/")

foreach(_releaselib ${_LIB_LIST})
    get_filename_component(_libname ${_releaselib} NAME_WE)
    
    if(TARGET @fetch_package_name@::${_libname})
        return()
    endif()

    set(_includedir "${_releaselib}/../../../include")
    cmake_path(NORMAL_PATH _includedir)
    if(NOT EXISTS ${_includedir})
        unset(_includedir)
    endif()

    file(GLOB_RECURSE _debuglib_list "${@fetch_package_name@_ROOT_DIR}*/${_libname}d.lib" "${@fetch_package_name@_ROOT_DIR}*/${_libname}.lib")
    list(FILTER _debuglib_list INCLUDE REGEX "/[Dd]ebug/")
    list(POP_FRONT _debuglib_list _debuglib)

    STRING(REPLACE ".lib" ".dll" _releasedll "${_releaselib}")
    STRING(REPLACE ".lib" ".dll" _debugdll "${_debuglib}")

    if(EXISTS "${_releasedll}")
        if(NOT EXISTS "${_debuglib}" OR NOT EXISTS "${_debugdll}")
            set(_debuglib "${_releaselib}")
            set(_debugdll "${_releasedll}")
        endif()

        list(REMOVE_ITEM _DLL_LIST "${_releasedll}")

        add_library(@fetch_package_name@::${_libname} SHARED IMPORTED)
        if(DEFINED _includedir)
            set_target_properties(@fetch_package_name@::${_libname} PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "${_includedir}")
        endif()
        set_target_properties(@fetch_package_name@::${_libname} PROPERTIES
            IMPORTED_CONFIGURATIONS "RELEASE;DEBUG"
            IMPORTED_IMPLIB_RELEASE "${_releaselib}"
            IMPORTED_LOCATION_RELEASE "${_releasedll}"
            IMPORTED_IMPLIB_DEBUG "${_debuglib}"
            IMPORTED_LOCATION_DEBUG "${_debugdll}"
        )
    else()
        if(NOT EXISTS "${_debuglib}")
            set(_debuglib "${_releaselib}")
        endif()

        add_library(@fetch_package_name@::${_libname} STATIC IMPORTED)
        if(DEFINED _includedir)
            set_target_properties(@fetch_package_name@::${_libname} PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "${_includedir}")
        endif()
        set_target_properties(@fetch_package_name@::${_libname} PROPERTIES
            IMPORTED_CONFIGURATIONS "RELEASE;DEBUG"
            IMPORTED_LINK_INTERFACE_LANGUAGES_RELEASE "CXX"
            IMPORTED_LOCATION_RELEASE "${_releaselib}"
            IMPORTED_LINK_INTERFACE_LANGUAGES_DEBUG "CXX"
            IMPORTED_LOCATION_DEBUG "${_debuglib}"
        )
    endif()

    list(APPEND @fetch_package_name@_LIBRARIES "@fetch_package_name@::${_libname}")
endforeach()

foreach(_releasedll ${_DLL_LIST})
    get_filename_component(_libname ${_releasedll} NAME_WE)
    
    if(TARGET @fetch_package_name@::${_libname})
        return()
    endif()

    file(GLOB_RECURSE _debugdll_list "${@fetch_package_name@_ROOT_DIR}*/${_libname}d.dll" "${@fetch_package_name@_ROOT_DIR}*/${_libname}.dll")
    list(FILTER _debugdll_list INCLUDE REGEX "/[Dd]ebug/")
    list(POP_FRONT _debugdll_list _debugdll)

    if(NOT EXISTS "${_debugdll}")
        set(_debugdll "${_releasedll}")
    endif()

    add_library(@fetch_package_name@::${_libname} MODULE IMPORTED)
    set_target_properties(@fetch_package_name@::${_libname} PROPERTIES
        IMPORTED_CONFIGURATIONS "RELEASE;DEBUG"
        IMPORTED_LOCATION_RELEASE "${_releasedll}"
        IMPORTED_LOCATION_DEBUG "${_debugdll}"
    )

    list(APPEND @fetch_package_name@_PLUGINS "@fetch_package_name@::${_libname}")
endforeach()

if(NOT @fetch_package_name@_LIBRARIES)
    file(GLOB_RECURSE _INCLUDE_DIR_LIST LIST_DIRECTORIES true "${@fetch_package_name@_ROOT_DIR}/*include")
    list(FILTER _INCLUDE_DIR_LIST INCLUDE REGEX "/include$")
    foreach(_includedir ${_INCLUDE_DIR_LIST})
        if(IS_DIRECTORY  ${_includedir})
            set(_targetdir "${_includedir}/..")
            cmake_path(NORMAL_PATH _targetdir)
            string(REGEX REPLACE "/$" "" _targetdir "${_targetdir}")
            string(REGEX REPLACE "/Delivery$" "" _targetdir "${_targetdir}")
            get_filename_component(_libname ${_targetdir} NAME)

            if(TARGET @fetch_package_name@::${_libname})
                return()
            endif()

            add_library(@fetch_package_name@::${_libname} INTERFACE IMPORTED)
            set_target_properties(@fetch_package_name@::${_libname} PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "${_includedir}")
        endif()
    endforeach()
endif()

# handle the QUIETLY and REQUIRED arguments and set xxx_FOUND to TRUE if
# all listed variables are TRUE
include(FindPackageHandleStandardArgs)
find_package_handle_standard_args(@fetch_package_name@ DEFAULT_MSG
    @fetch_package_name@_ROOT_DIR @fetch_package_name@_LIBRARIES)
